# Руководство по созданию релиза

## ✅ Что уже сделано:

1. ✅ Установлен `electron-updater`
2. ✅ Создан модуль `updater.js`
3. ✅ Добавлена проверка обновлений в `main.cjs`
4. ✅ Настроен `package.json` для GitHub Releases

## 🚀 Создание первого релиза

### Шаг 1: Установите electron-builder (если еще не установлен)

```powershell
npm install electron-builder --save-dev
```

### Шаг 2: Соберите приложение

```powershell
npm run build
```

Это создаст установщик в папке `release/`

### Шаг 3: Создайте релиз на GitHub

1. Откройте https://github.com/kosenkomaks1999-dotcom/vibechat/releases
2. Нажмите "Create a new release"
3. Заполните:
   - **Tag version**: `v1.0.0` (обязательно с буквой v!)
   - **Release title**: `VibeChat v1.0.0`
   - **Description**: Опишите что нового
4. Загрузите файлы из папки `release/`:
   - `VibeChat Setup 1.0.0.exe` (установщик)
   - `latest.yml` (файл с информацией о версии)
5. Нажмите "Publish release"

### Шаг 4: Создайте GitHub Token для публикации

1. GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. "Generate new token (classic)"
4. Название: `VibeChat Releases`
5. Права: `repo` (все галочки)
6. Скопируйте токен

### Шаг 5: Настройте автоматическую публикацию

Создайте файл `.env` в корне проекта:

```
GH_TOKEN=ваш_github_token
```

Теперь можно публиковать автоматически:

```powershell
npm run publish
```

## 📝 Создание обновлений

### 1. Обновите версию в package.json

```json
{
  "version": "1.0.1"
}
```

### 2. Зафиксируйте изменения

```powershell
git add .
git commit -m "Update to v1.0.1"
git push
```

### 3. Соберите и опубликуйте

```powershell
npm run publish
```

Или вручную:

```powershell
npm run build
```

Затем создайте новый релиз на GitHub с тегом `v1.0.1`

## 🧪 Тестирование автообновления

1. Установите версию 1.0.0
2. Создайте релиз 1.0.1 на GitHub
3. Запустите приложение версии 1.0.0
4. Через 3 секунды появится уведомление об обновлении
5. Нажмите "Да" для скачивания
6. После скачивания нажмите "Перезапустить"
7. Приложение обновится до версии 1.0.1

## ⚠️ Важно!

- Версия в `package.json` должна совпадать с тегом релиза
- Тег релиза должен начинаться с `v` (например, `v1.0.0`)
- Обязательно загружайте файл `latest.yml` в релиз
- Для Windows загружайте `.exe` установщик

## 🔧 Команды

```powershell
# Запуск приложения
npm start

# Сборка для Windows
npm run build:win

# Сборка и публикация
npm run publish
```

## 📦 Структура релиза

После сборки в папке `release/` будут:
- `VibeChat Setup 1.0.0.exe` - установщик
- `latest.yml` - информация о версии
- `win-unpacked/` - распакованные файлы

Загружайте на GitHub:
- ✅ `VibeChat Setup 1.0.0.exe`
- ✅ `latest.yml`

## 🐛 Возможные проблемы

### "Cannot find module 'electron'"
Решение: `npm install`

### "GH_TOKEN is not set"
Решение: Создайте файл `.env` с токеном

### Обновление не находится
Решение: Проверьте что:
- Тег релиза начинается с `v`
- Файл `latest.yml` загружен
- Версия в `package.json` меньше версии релиза
