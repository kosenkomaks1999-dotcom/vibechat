# VibeChat - Voice Chat Application

Современное десктопное приложение для голосового и текстового общения с поддержкой комнат и системы друзей.

## 🚀 Возможности

- **Голосовой чат** через WebRTC (P2P соединения)
- **Текстовый чат** с поддержкой файлов (изображения, аудио, видео)
- **Система авторизации** (email/никнейм)
- **Система друзей** с запросами и уведомлениями
- **Комнаты** с уникальными ID и управлением участниками
- **Профили пользователей** с аватарами и настройками
- **Настройка устройств** (микрофон, динамики)
- **Детекция речи** с визуальной индикацией
- **Настраиваемые фоны** и параметры интерфейса
- **Встроенная консоль разработчика**

## 📋 Требования

- Node.js 16+ 
- npm или yarn
- Firebase проект (для бэкенда)

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd VibeChat
```

2. Установите зависимости:
```bash
cd resources/app
npm install
```

3. Настройте Firebase:
   - Создайте файл `config/firebase.config.js` на основе `config/firebase.config.example.js`
   - Добавьте ваши Firebase credentials

## 🎮 Запуск

### Electron (рекомендуется)
```bash
npm start
```

### Браузер (через HTTP сервер)
```bash
npm run dev
```

### Браузер (через file://)
Просто откройте `index.html` в браузере - загрузится старая версия `app.js`, которая работает без HTTP сервера.

## 📁 Структура проекта

```
resources/app/
├── src/
│   ├── app/                    # Модули приложения
│   │   ├── app-state.js       # Управление состоянием
│   │   ├── auth-handlers.js   # Обработчики авторизации
│   │   ├── room-handlers.js   # Обработчики комнат
│   │   ├── chat-handlers.js   # Обработчики чата
│   │   ├── ui-handlers.js     # Обработчики UI
│   │   └── friends-handlers.js # Обработчики друзей
│   ├── modules/                # Основные модули
│   │   ├── auth.js            # Авторизация
│   │   ├── chat.js             # Чат
│   │   ├── webrtc.js           # WebRTC соединения
│   │   ├── firebase.js         # Работа с Firebase
│   │   ├── friends.js          # Система друзей
│   │   └── ...
│   ├── utils/                  # Утилиты
│   │   ├── dom-cache.js        # Кэш DOM элементов
│   │   ├── lazy-loader.js      # Lazy loading модулей
│   │   ├── file-security.js   # Проверка безопасности файлов
│   │   └── security.js        # Валидация и безопасность
│   └── app.js                  # Главный файл приложения
├── config/                     # Конфигурация
│   └── firebase.config.js      # Firebase настройки
├── assets/                     # Ресурсы
│   ├── icons/                  # Иконки
│   └── background*.jpg        # Фоны
├── index.html                  # Главная страница
├── style.css                   # Стили
└── package.json                # Зависимости
```

## 🔧 Разработка

### Архитектура

Проект использует модульную архитектуру с разделением ответственности:

- **Модули** (`src/modules/`) - основная бизнес-логика
- **Обработчики** (`src/app/`) - обработка событий и UI взаимодействий
- **Утилиты** (`src/utils/`) - вспомогательные функции

### Оптимизация производительности

Проект использует несколько техник оптимизации:

1. **DOM кэширование** - элементы DOM кэшируются для избежания повторных запросов
2. **Lazy loading** - модули загружаются только когда нужны
3. **Проверка безопасности файлов** - проверка магических чисел файлов

Пример использования:

```javascript
import { getElementById } from '../utils/dom-cache.js';
import { loadModule } from '../utils/lazy-loader.js';

// Кэшированный доступ к DOM
const element = getElementById('myElement');

// Lazy loading модуля
const module = await loadModule('./modules/my-module.js');
```

### Безопасность

- **XSS защита** - все пользовательские данные экранируются через `escapeHtml()`
- **Валидация файлов** - проверка магических чисел для определения реального типа файла
- **Валидация входных данных** - проверка длины и формата никнеймов, сообщений
- **Rate limiting** - ограничение частоты отправки сообщений
- **Firebase Security Rules** - правила безопасности настроены

Пример проверки файла:

```javascript
import { validateFile } from '../utils/file-security.js';

const validation = await validateFile(file, {
  allowedTypes: ['image/png', 'image/jpeg'],
  maxSize: 10 * 1024 * 1024, // 10MB
  strictTypeCheck: true // Проверка магических чисел
});

if (!validation.valid) {
  console.error(validation.error);
}
```

### Обработка ошибок

Все ошибки обрабатываются через централизованный `error-handler`:

```javascript
import { errorHandler, ErrorCodes } from './modules/error-handler.js';

try {
  // код
} catch (error) {
  errorHandler.handle(error, { operation: 'operationName', context: 'value' });
}
```

## 🧪 Тестирование

Запуск тестов:
```bash
npm test
```

Запуск тестов с покрытием:
```bash
npm run test:coverage
```

Запуск тестов в watch режиме:
```bash
npm run test:watch
```

## 📦 Сборка

### Сборка для Windows
```bash
npm run package-win
```

## 📚 API Документация

### Модули

#### AuthManager
Управление авторизацией пользователей.

```javascript
import { AuthManager } from './modules/auth.js';

const authManager = new AuthManager(auth);
const result = await authManager.signIn(email, password);
```

**Методы:**
- `signIn(email, password, getEmailByNicknameFn)` - Вход пользователя
- `signUp(email, password)` - Регистрация пользователя
- `signOut()` - Выход пользователя
- `getCurrentUser()` - Получить текущего пользователя
- `onAuthStateChanged(callback)` - Подписка на изменения авторизации

#### ChatManager
Управление чатом и сообщениями.

```javascript
import { ChatManager } from './modules/chat.js';

const chat = new ChatManager(roomRef, myNickname, userId, db);
chat.initElements(chatMessages, chatInput, fileInput);
await chat.attachFile(file, showToast);
```

**Методы:**
- `initElements(chatMessages, chatInput, fileInput)` - Инициализация элементов
- `sendMessage(showToast)` - Отправка сообщения
- `attachFile(file, showToast)` - Прикрепление файла
- `clear()` - Очистка чата
- `displayMessage(message)` - Отображение сообщения

#### WebRTCManager
Управление WebRTC соединениями.

```javascript
import { WebRTCManager } from './modules/webrtc.js';

const webrtc = new WebRTCManager(roomRef, myId, onStreamReceived);
await webrtc.initMicrophone(deviceId, muted);
webrtc.createPeer(otherId, initiator);
```

**Методы:**
- `initMicrophone(deviceId, muted)` - Инициализация микрофона
- `createPeer(otherId, initiator)` - Создание P2P соединения
- `toggleMute(muted)` - Переключение микрофона
- `toggleSpeaker()` - Переключение динамиков
- `setUserVolume(userId, volume)` - Установка громкости
- `cleanup()` - Очистка всех соединений

#### FriendsManager
Управление системой друзей.

```javascript
import { FriendsManager } from './modules/friends.js';

const friendsManager = new FriendsManager(db, authManager, ui, userId);
await friendsManager.loadFriends();
await friendsManager.sendFriendRequest(nickname);
```

**Методы:**
- `loadFriends()` - Загрузка списка друзей
- `sendFriendRequest(nickname)` - Отправка запроса в друзья
- `acceptFriendRequest(friendId)` - Принятие запроса
- `rejectFriendRequest(friendId)` - Отклонение запроса
- `loadNotifications()` - Загрузка уведомлений

### Утилиты

#### DOM Cache
Кэширование DOM элементов для оптимизации.

```javascript
import { getElementById, getElementsByIds } from './utils/dom-cache.js';

const element = getElementById('myElement');
const elements = getElementsByIds(['id1', 'id2', 'id3']);
```

#### Lazy Loader
Lazy loading модулей.

```javascript
import { loadModule, preloadModule } from './utils/lazy-loader.js';

// Загрузка модуля по требованию
const module = await loadModule('./modules/my-module.js');

// Предзагрузка модуля
await preloadModule('./modules/my-module.js');
```

#### File Security
Проверка безопасности файлов.

```javascript
import { validateFile, detectFileType, getFileInfo } from './utils/file-security.js';

// Проверка файла
const validation = await validateFile(file, {
  allowedTypes: ['image/png', 'image/jpeg'],
  maxSize: 10 * 1024 * 1024,
  strictTypeCheck: true
});

// Определение типа файла
const fileType = await detectFileType(file);

// Получение информации о файле
const info = await getFileInfo(file);
```

## 🔐 Безопасность

### Firebase Security Rules

Убедитесь, что правила безопасности Firebase настроены правильно. См. `config/firebase-security-rules.example.json`.

### Проверка файлов

Все загружаемые файлы проверяются на:
- Размер (максимум 10MB)
- Тип (по MIME и магическим числам)
- Безопасность содержимого

### Валидация данных

Все пользовательские данные валидируются:
- Никнеймы: 1-15 символов, только буквы, цифры, дефис и подчеркивание
- Сообщения: максимум 200 символов
- Rate limiting: ограничение частоты отправки сообщений

## 🐛 Отладка

### Встроенная консоль разработчика

В приложении есть встроенная консоль разработчика. Нажмите кнопку 📟 в title bar для открытия.

### Логирование

Логи записываются в файлы в папке `logs/`:
- `vibechat-YYYY-MM-DD.txt` - основные логи
- `rooms-YYYY-MM-DD.txt` - логи комнат

## 📝 Примеры использования

### Создание комнаты

```javascript
import { RoomHandlers } from './app/room-handlers.js';

const roomHandlers = new RoomHandlers(state, ui, authManager, db, ...);
await roomHandlers.createRoomWithName(roomId, roomName);
```

### Отправка сообщения

```javascript
import { ChatHandlers } from './app/chat-handlers.js';

const chatHandlers = new ChatHandlers(state, ui, chat);
chatHandlers.sendMessage();
```

### Добавление друга

```javascript
import { FriendsHandlers } from './app/friends-handlers.js';

const friendsHandlers = new FriendsHandlers(state, ui, friendsManager, roomHandlers);
await friendsHandlers.handleAddFriend();
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

См. файл `LICENSE` для подробностей.

## 🙏 Благодарности

- Firebase за бэкенд инфраструктуру
- SimplePeer за WebRTC библиотеку
- Electron за фреймворк для десктопных приложений

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-01-08
