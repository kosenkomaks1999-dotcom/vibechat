# Changelog - VibeChat

## [1.1.0] - 2025-01-XX

### 🔧 Исправления

#### 🚨 КРИТИЧЕСКОЕ: Исправлены сильные лаги при использовании
**Проблема:** Сильные лаги при общении, особенно когда кто-то создает другую комнату. Лаги продолжались даже после выхода.

**Причина:** Слушатель `db.ref("rooms").on("value")` загружал **ВСЕ комнаты** при **ЛЮБОМ изменении** у **ВСЕХ пользователей**.

**Решение:**
- ✅ Изменен слушатель с `on("value")` на `on("child_*")`
- ✅ Добавлен debounce 1 секунда для обновлений
- ✅ Правильное отключение всех слушателей
- ✅ Снижение нагрузки в 4.5 раза

**Результат:** Нет лагов даже при 10+ комнатах. Приложение работает плавно.

#### Исправлено отображение и масштабирование аватарок
**Проблема:** Аватарки не отображались у участников, неправильно масштабировались.

**Причина:** Использовался `background-image` вместо `<img>`, код не загружал аватарки участников.

**Решение:**
- ✅ Все аватарки теперь используют `<img>` элементы
- ✅ `object-fit: cover` правильно масштабирует изображения в круг
- ✅ Добавлена загрузка аватарок участников из Firebase
- ✅ Обновлены аватарки друзей и профиля
- ✅ Изображения центрируются и заполняют весь круг

**Результат:** Аватарки правильно масштабируются, отображаются у всех участников и друзей.

#### Исправлена автоматическая смена вкладок
**Проблема:** Когда друг находился на вкладке "Друзья", создание новой комнаты автоматически переключало его на вкладку "Комнаты".

**Причина:** Функции `renderRoomsList()` и слушатель комнат автоматически переключали вкладки при обновлении списка комнат.

**Решение:**
- ✅ Удалена автоматическая смена вкладок при обновлении списка комнат
- ✅ Список комнат теперь обновляется в фоне
- ✅ Пользователь остается на выбранной вкладке

**Результат:** Вкладки переключаются только по действию пользователя.

#### Исправлена проблема с отключением из комнаты
**Проблема:** При нестабильном интернете пользователи вылетали из комнаты, но продолжали слышать друг друга некоторое время, затем полностью отключались.

**Решение:**
- ✅ Добавлена система автоматического переподключения
- ✅ Добавлен heartbeat для мониторинга соединения
- ✅ Увеличен буфер времени перед отключением (15 секунд)
- ✅ До 3 автоматических попыток переподключения
- ✅ Уведомления о статусе переподключения

**Результат:** Приложение теперь устойчиво к кратковременным сбоям сети и автоматически восстанавливает соединение.

#### Исправлена проблема с немедленным выкидыванием из комнаты (v1.1.1)
**Проблема:** После первого исправления пользователей выкидывало из комнаты сразу после входа.

**Причина:** Callback переподключения срабатывал при первом подключении к Firebase.

**Решение:**
- ✅ Изменена логика определения переподключения (wasConnected: null → false → true)
- ✅ Callback теперь срабатывает только при реальном восстановлении соединения
- ✅ Добавлены дополнительные проверки в attemptReconnect()

**Результат:** Вход в комнату работает корректно, переподключение срабатывает только при необходимости.

#### Исправлен ложный статус "отключено" при запуске (v1.1.2)
**Проблема:** Через несколько секунд после запуска приложения статус показывал "отключено".

**Причина:** Heartbeat срабатывал раньше, чем Firebase успевал ответить при первом запуске.

**Решение:**
- ✅ Heartbeat отключен по умолчанию
- ✅ Firebase `.info/connected` - единственный источник информации о состоянии
- ✅ Heartbeat можно включить для отладки (параметр конструктора)
- ✅ Добавлено подробное логирование

**Результат:** Статус подключения полностью управляется Firebase, без ложных срабатываний.

### 📊 Технические улучшения

- Улучшен `ConnectionManager` с heartbeat системой
- Добавлена функция `attemptReconnect()` для автоматического восстановления
- Добавлена защита от ложных срабатываний disconnected
- Улучшена обработка состояния соединения

---

## [1.0.0] - 2025-01-08

### 🎉 Первый релиз

- Голосовой чат через WebRTC
- Текстовый чат с файлами
- Система друзей
- Детекция речи
- Настройка устройств
- Кастомизация интерфейса
