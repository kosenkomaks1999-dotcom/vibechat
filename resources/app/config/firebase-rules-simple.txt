УПРОЩЕННЫЕ ПРАВИЛА (для начала работы):

Если правила выше вызывают ошибки, используйте эти упрощенные правила:

{
  "rules": {
    "nicknames": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "users": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "rooms": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}

ВАЖНО: Эти упрощенные правила менее безопасны, но позволят приложению работать.
Рекомендуется использовать полные правила из firebase-rules.txt после тестирования.



