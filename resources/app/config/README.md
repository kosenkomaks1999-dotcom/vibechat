# Конфигурация Firebase

## Настройка

1. Скопируйте файл `firebase.config.example.js` в `firebase.config.js`:
   ```bash
   cp firebase.config.example.js firebase.config.js
   ```

2. Откройте `firebase.config.js` и заполните своими данными Firebase:
   ```javascript
   const firebaseConfig = {
     apiKey: "ВАШ_API_KEY",
     authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
     databaseURL: "https://ВАШ_PROJECT_ID-default-rtdb.firebaseio.com",
     projectId: "ВАШ_PROJECT_ID",
   };
   ```

## Где найти данные Firebase?

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Project Settings** (⚙️)
4. В разделе **Your apps** найдите ваше веб-приложение
5. Скопируйте значения из конфигурации

## Важно!

- **НЕ коммитьте** `firebase.config.js` в репозиторий!
- Файл уже добавлен в `.gitignore`
- Используйте `firebase.config.example.js` как шаблон для других разработчиков

## Настройка Authentication

Для работы системы регистрации и авторизации необходимо включить Email/Password Authentication:

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **Sign-in method**
4. Нажмите на **Email/Password**
5. Включите **Email/Password** и нажмите **Save**
6. (Опционально) Отключите **Email link (passwordless sign-in)**, если не планируете использовать

Теперь пользователи смогут регистрироваться и входить в приложение через email и пароль.

## Настройка Security Rules для никнеймов

Для работы системы никнеймов необходимо настроить правила безопасности Firebase.

**📖 Подробная пошаговая инструкция:** См. файл [FIREBASE_RULES_SETUP.md](FIREBASE_RULES_SETUP.md)

**Краткая инструкция:**
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Realtime Database** → **Rules** (вкладка "Правила")
4. Скопируйте содержимое файла `firebase-security-rules.example.json` в поле правил
5. Нажмите **"Опубликовать"** (Publish)

Эти правила обеспечивают:
- Только авторизованные пользователи могут читать и записывать никнеймы
- Пользователь может изменить только свой никнейм
- Валидация длины никнейма (до 15 символов)
- Защита от дублирования никнеймов

## Безопасность

Хотя Firebase API ключи не являются критически секретными (они ограничены доменом через Firebase Security Rules), рекомендуется:
- Не делиться конфигурацией публично
- Настроить Firebase Security Rules для ограничения доступа
- Использовать разные проекты для разработки и продакшена
- Настроить правила аутентификации в Firebase Console

