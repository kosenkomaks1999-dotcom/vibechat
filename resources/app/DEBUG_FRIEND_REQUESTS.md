# Отладка системы запросов в друзья

## Инструкция по отладке

1. **Откройте консоль браузера (F12)**

2. **Проверьте инициализацию FriendsManager:**
   - Должны быть логи: `🔔 Инициализация FriendsManager...`
   - Должен быть лог: `✅ FriendsManager полностью инициализирован`

3. **Проверьте запуск слушателя уведомлений:**
   - Должен быть лог: `🔔 Запуск слушателя уведомлений для пользователя: [userId]`
   - Должен быть лог: `✅ Слушатель уведомлений успешно запущен`

4. **При ПКМ по участнику:**
   - Должен быть лог: `🔵🔵🔵 ПКМ по участнику - Сохранение данных в контекстное меню`
   - Проверьте, что `firebaseUserId` сохраняется (если нет - будет использован никнейм)

5. **При нажатии "Добавить в друзья":**
   - Должен быть лог: `🔵🔵🔵🔵🔵 КНОПКА "ДОБАВИТЬ В ДРУЗЬЯ" НАЖАТА!`
   - Проверьте данные из dataset
   - Проверьте результат отправки запроса

6. **Проверьте сохранение в Firebase:**
   - Должен быть лог: `💾 Сохранение запроса в Firebase`
   - Должен быть лог: `✅ Запрос успешно сохранен в Firebase`
   - Если есть ошибка - проверьте код ошибки

7. **Проверьте получение уведомления:**
   - Должен быть лог: `🔔 Новый запрос в друзья получен!`
   - Должен быть лог: `🔔 Показываем toast уведомление`

## Возможные проблемы:

1. **Firebase userId не сохраняется в комнате:**
   - Проверьте, что пользователь авторизован при создании/присоединении к комнате
   - Проверьте логи: `⚠️ ВНИМАНИЕ: Firebase userId не найден!`

2. **Запрос не сохраняется в Firebase:**
   - Проверьте правила Firebase (должны разрешать запись отправителю)
   - Проверьте код ошибки в консоли
   - Проверьте, что пользователь авторизован

3. **Уведомление не приходит:**
   - Проверьте, что слушатель запущен
   - Проверьте, что запрос сохранился в Firebase
   - Проверьте, что uiManager передан в FriendsManager
   - Проверьте логи: `⚠️ uiManager не доступен для показа toast уведомления`

4. **Правила Firebase:**
   - Убедитесь, что правила позволяют запись отправителю: `auth.uid == $fromUserId`
   - Убедитесь, что правила позволяют чтение получателю: `$userId == auth.uid`

## Тестирование:

1. Создайте две учетные записи
2. Войдите в обе учетные записи в разных браузерах/окнах
3. Присоединитесь к одной комнате с обеих учетных записей
4. Кликните ПКМ по участнику и выберите "Добавить в друзья"
5. Проверьте консоль на наличие ошибок
6. Проверьте, что запрос сохранился в Firebase Console
7. Проверьте, что получатель получил уведомление


